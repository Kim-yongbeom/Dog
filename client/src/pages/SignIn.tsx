import React from "react";
import styled from "@emotion/styled/macro";

const Base = styled.div`
  padding-top: 5rem;
`;

const SignIn: React.FC = () => {
  return <Base>zzzzzz</Base>;
};

export default SignIn;
